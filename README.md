# Молодёжный культурный центр ВитГТК

Веб-сайт для Молодёжного культурного центра Витебского государственного технологического колледжа.

## Описание

Сайт предоставляет информацию о центре, студиях, мероприятиях и позволяет пользователям регистрироваться на события.

## Основные возможности

- 📋 Информация о центре и студиях
- 🎭 Афиша мероприятий с подробными статьями
- 📝 Система регистрации пользователей
- 🖼️ Галерея фотографий для статей
- 🔐 Админ-панель для управления контентом

## Структура проекта

```
ProjectFI/
├── api/                    # Backend (Flask)
│   ├── server.py          # Основной серверный файл
│   ├── registrations/     # Регистрации пользователей (игнорируется в git)
│   └── ...
├── *.html                 # HTML страницы
├── *.js                   # JavaScript файлы
├── *.css                  # Стили
└── *.json                 # Данные (события, статьи)

```

## Технологии

- **Frontend**: HTML, CSS, JavaScript
- **Backend**: Flask (Python)
- **Хранение данных**: JSON файлы

## Установка и запуск

### Требования

- Python 3.7+
- Flask
- flask-cors

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск сервера

```bash
cd api
python server.py
```

Сервер будет доступен по адресу: `http://localhost:5000`

### Открытие сайта

Откройте файл `Index.html` в браузере или используйте локальный веб-сервер.

## API эндпоинты

- `POST /api/register` - Регистрация пользователя
- `POST /api/user-login` - Вход пользователя
- `POST /api/login` - Вход администратора
- `GET /api/events` - Получить события (требуется авторизация)
- `POST /api/events` - Сохранить события (требуется авторизация)
- `GET /api/articles` - Получить статьи (требуется авторизация)
- `POST /api/articles` - Сохранить статьи (требуется авторизация)

## Учетные данные администратора

По умолчанию:
- Имя пользователя: `admin`
- Пароль: `admin123`

⚠️ **Важно**: Измените пароль в `api/server.py` перед развертыванием в продакшене!

## Лицензия

© ВитГТК — МКЦ 2025

